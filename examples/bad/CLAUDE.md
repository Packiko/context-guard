# CLAUDE.md

Project instructions for the Acme Monolith. This file has grown out of control:
it is over 300 lines and is missing the context-guard rules block near the top.
(Deliberately non-compliant — used to test context-audit / context-fix.)

## Overview

Everything you could possibly want to know about this project, inline, forever.

## Architecture notes (should mostly be extracted to docs/architecture.md)
- Component #1: handles a subsystem; see code for details. Padding line to push this file past the 300-line ceiling so the audit flags it.
- Component #2: handles a subsystem; see code for details. Padding line to push this file past the 300-line ceiling so the audit flags it.
- Component #3: handles a subsystem; see code for details. Padding line to push this file past the 300-line ceiling so the audit flags it.
- Component #4: handles a subsystem; see code for details. Padding line to push this file past the 300-line ceiling so the audit flags it.
- Component #5: handles a subsystem; see code for details. Padding line to push this file past the 300-line ceiling so the audit flags it.
- Component #6: handles a subsystem; see code for details. Padding line to push this file past the 300-line ceiling so the audit flags it.
- Component #7: handles a subsystem; see code for details. Padding line to push this file past the 300-line ceiling so the audit flags it.
- Component #8: handles a subsystem; see code for details. Padding line to push this file past the 300-line ceiling so the audit flags it.
- Component #9: handles a subsystem; see code for details. Padding line to push this file past the 300-line ceiling so the audit flags it.
- Component #10: handles a subsystem; see code for details. Padding line to push this file past the 300-line ceiling so the audit flags it.
- Component #11: handles a subsystem; see code for details. Padding line to push this file past the 300-line ceiling so the audit flags it.
- Component #12: handles a subsystem; see code for details. Padding line to push this file past the 300-line ceiling so the audit flags it.
- Component #13: handles a subsystem; see code for details. Padding line to push this file past the 300-line ceiling so the audit flags it.
- Component #14: handles a subsystem; see code for details. Padding line to push this file past the 300-line ceiling so the audit flags it.
- Component #15: handles a subsystem; see code for details. Padding line to push this file past the 300-line ceiling so the audit flags it.
- Component #16: handles a subsystem; see code for details. Padding line to push this file past the 300-line ceiling so the audit flags it.
- Component #17: handles a subsystem; see code for details. Padding line to push this file past the 300-line ceiling so the audit flags it.
- Component #18: handles a subsystem; see code for details. Padding line to push this file past the 300-line ceiling so the audit flags it.
- Component #19: handles a subsystem; see code for details. Padding line to push this file past the 300-line ceiling so the audit flags it.
- Component #20: handles a subsystem; see code for details. Padding line to push this file past the 300-line ceiling so the audit flags it.
- Component #21: handles a subsystem; see code for details. Padding line to push this file past the 300-line ceiling so the audit flags it.
- Component #22: handles a subsystem; see code for details. Padding line to push this file past the 300-line ceiling so the audit flags it.
- Component #23: handles a subsystem; see code for details. Padding line to push this file past the 300-line ceiling so the audit flags it.
- Component #24: handles a subsystem; see code for details. Padding line to push this file past the 300-line ceiling so the audit flags it.
- Component #25: handles a subsystem; see code for details. Padding line to push this file past the 300-line ceiling so the audit flags it.
- Component #26: handles a subsystem; see code for details. Padding line to push this file past the 300-line ceiling so the audit flags it.
- Component #27: handles a subsystem; see code for details. Padding line to push this file past the 300-line ceiling so the audit flags it.
- Component #28: handles a subsystem; see code for details. Padding line to push this file past the 300-line ceiling so the audit flags it.
- Component #29: handles a subsystem; see code for details. Padding line to push this file past the 300-line ceiling so the audit flags it.
- Component #30: handles a subsystem; see code for details. Padding line to push this file past the 300-line ceiling so the audit flags it.
- Component #31: handles a subsystem; see code for details. Padding line to push this file past the 300-line ceiling so the audit flags it.
- Component #32: handles a subsystem; see code for details. Padding line to push this file past the 300-line ceiling so the audit flags it.
- Component #33: handles a subsystem; see code for details. Padding line to push this file past the 300-line ceiling so the audit flags it.
- Component #34: handles a subsystem; see code for details. Padding line to push this file past the 300-line ceiling so the audit flags it.
- Component #35: handles a subsystem; see code for details. Padding line to push this file past the 300-line ceiling so the audit flags it.
- Component #36: handles a subsystem; see code for details. Padding line to push this file past the 300-line ceiling so the audit flags it.
- Component #37: handles a subsystem; see code for details. Padding line to push this file past the 300-line ceiling so the audit flags it.
- Component #38: handles a subsystem; see code for details. Padding line to push this file past the 300-line ceiling so the audit flags it.
- Component #39: handles a subsystem; see code for details. Padding line to push this file past the 300-line ceiling so the audit flags it.
- Component #40: handles a subsystem; see code for details. Padding line to push this file past the 300-line ceiling so the audit flags it.
- Component #41: handles a subsystem; see code for details. Padding line to push this file past the 300-line ceiling so the audit flags it.
- Component #42: handles a subsystem; see code for details. Padding line to push this file past the 300-line ceiling so the audit flags it.
- Component #43: handles a subsystem; see code for details. Padding line to push this file past the 300-line ceiling so the audit flags it.
- Component #44: handles a subsystem; see code for details. Padding line to push this file past the 300-line ceiling so the audit flags it.
- Component #45: handles a subsystem; see code for details. Padding line to push this file past the 300-line ceiling so the audit flags it.
- Component #46: handles a subsystem; see code for details. Padding line to push this file past the 300-line ceiling so the audit flags it.
- Component #47: handles a subsystem; see code for details. Padding line to push this file past the 300-line ceiling so the audit flags it.
- Component #48: handles a subsystem; see code for details. Padding line to push this file past the 300-line ceiling so the audit flags it.
- Component #49: handles a subsystem; see code for details. Padding line to push this file past the 300-line ceiling so the audit flags it.
- Component #50: handles a subsystem; see code for details. Padding line to push this file past the 300-line ceiling so the audit flags it.
- Component #51: handles a subsystem; see code for details. Padding line to push this file past the 300-line ceiling so the audit flags it.
- Component #52: handles a subsystem; see code for details. Padding line to push this file past the 300-line ceiling so the audit flags it.
- Component #53: handles a subsystem; see code for details. Padding line to push this file past the 300-line ceiling so the audit flags it.
- Component #54: handles a subsystem; see code for details. Padding line to push this file past the 300-line ceiling so the audit flags it.
- Component #55: handles a subsystem; see code for details. Padding line to push this file past the 300-line ceiling so the audit flags it.
- Component #56: handles a subsystem; see code for details. Padding line to push this file past the 300-line ceiling so the audit flags it.
- Component #57: handles a subsystem; see code for details. Padding line to push this file past the 300-line ceiling so the audit flags it.
- Component #58: handles a subsystem; see code for details. Padding line to push this file past the 300-line ceiling so the audit flags it.
- Component #59: handles a subsystem; see code for details. Padding line to push this file past the 300-line ceiling so the audit flags it.
- Component #60: handles a subsystem; see code for details. Padding line to push this file past the 300-line ceiling so the audit flags it.
- Component #61: handles a subsystem; see code for details. Padding line to push this file past the 300-line ceiling so the audit flags it.
- Component #62: handles a subsystem; see code for details. Padding line to push this file past the 300-line ceiling so the audit flags it.
- Component #63: handles a subsystem; see code for details. Padding line to push this file past the 300-line ceiling so the audit flags it.
- Component #64: handles a subsystem; see code for details. Padding line to push this file past the 300-line ceiling so the audit flags it.
- Component #65: handles a subsystem; see code for details. Padding line to push this file past the 300-line ceiling so the audit flags it.
- Component #66: handles a subsystem; see code for details. Padding line to push this file past the 300-line ceiling so the audit flags it.
- Component #67: handles a subsystem; see code for details. Padding line to push this file past the 300-line ceiling so the audit flags it.
- Component #68: handles a subsystem; see code for details. Padding line to push this file past the 300-line ceiling so the audit flags it.
- Component #69: handles a subsystem; see code for details. Padding line to push this file past the 300-line ceiling so the audit flags it.
- Component #70: handles a subsystem; see code for details. Padding line to push this file past the 300-line ceiling so the audit flags it.
- Component #71: handles a subsystem; see code for details. Padding line to push this file past the 300-line ceiling so the audit flags it.
- Component #72: handles a subsystem; see code for details. Padding line to push this file past the 300-line ceiling so the audit flags it.
- Component #73: handles a subsystem; see code for details. Padding line to push this file past the 300-line ceiling so the audit flags it.
- Component #74: handles a subsystem; see code for details. Padding line to push this file past the 300-line ceiling so the audit flags it.
- Component #75: handles a subsystem; see code for details. Padding line to push this file past the 300-line ceiling so the audit flags it.
- Component #76: handles a subsystem; see code for details. Padding line to push this file past the 300-line ceiling so the audit flags it.
- Component #77: handles a subsystem; see code for details. Padding line to push this file past the 300-line ceiling so the audit flags it.
- Component #78: handles a subsystem; see code for details. Padding line to push this file past the 300-line ceiling so the audit flags it.
- Component #79: handles a subsystem; see code for details. Padding line to push this file past the 300-line ceiling so the audit flags it.
- Component #80: handles a subsystem; see code for details. Padding line to push this file past the 300-line ceiling so the audit flags it.
- Component #81: handles a subsystem; see code for details. Padding line to push this file past the 300-line ceiling so the audit flags it.
- Component #82: handles a subsystem; see code for details. Padding line to push this file past the 300-line ceiling so the audit flags it.
- Component #83: handles a subsystem; see code for details. Padding line to push this file past the 300-line ceiling so the audit flags it.
- Component #84: handles a subsystem; see code for details. Padding line to push this file past the 300-line ceiling so the audit flags it.
- Component #85: handles a subsystem; see code for details. Padding line to push this file past the 300-line ceiling so the audit flags it.
- Component #86: handles a subsystem; see code for details. Padding line to push this file past the 300-line ceiling so the audit flags it.
- Component #87: handles a subsystem; see code for details. Padding line to push this file past the 300-line ceiling so the audit flags it.
- Component #88: handles a subsystem; see code for details. Padding line to push this file past the 300-line ceiling so the audit flags it.
- Component #89: handles a subsystem; see code for details. Padding line to push this file past the 300-line ceiling so the audit flags it.
- Component #90: handles a subsystem; see code for details. Padding line to push this file past the 300-line ceiling so the audit flags it.
- Component #91: handles a subsystem; see code for details. Padding line to push this file past the 300-line ceiling so the audit flags it.
- Component #92: handles a subsystem; see code for details. Padding line to push this file past the 300-line ceiling so the audit flags it.
- Component #93: handles a subsystem; see code for details. Padding line to push this file past the 300-line ceiling so the audit flags it.
- Component #94: handles a subsystem; see code for details. Padding line to push this file past the 300-line ceiling so the audit flags it.
- Component #95: handles a subsystem; see code for details. Padding line to push this file past the 300-line ceiling so the audit flags it.
- Component #96: handles a subsystem; see code for details. Padding line to push this file past the 300-line ceiling so the audit flags it.
- Component #97: handles a subsystem; see code for details. Padding line to push this file past the 300-line ceiling so the audit flags it.
- Component #98: handles a subsystem; see code for details. Padding line to push this file past the 300-line ceiling so the audit flags it.
- Component #99: handles a subsystem; see code for details. Padding line to push this file past the 300-line ceiling so the audit flags it.
- Component #100: handles a subsystem; see code for details. Padding line to push this file past the 300-line ceiling so the audit flags it.
- Component #101: handles a subsystem; see code for details. Padding line to push this file past the 300-line ceiling so the audit flags it.
- Component #102: handles a subsystem; see code for details. Padding line to push this file past the 300-line ceiling so the audit flags it.
- Component #103: handles a subsystem; see code for details. Padding line to push this file past the 300-line ceiling so the audit flags it.
- Component #104: handles a subsystem; see code for details. Padding line to push this file past the 300-line ceiling so the audit flags it.
- Component #105: handles a subsystem; see code for details. Padding line to push this file past the 300-line ceiling so the audit flags it.
- Component #106: handles a subsystem; see code for details. Padding line to push this file past the 300-line ceiling so the audit flags it.
- Component #107: handles a subsystem; see code for details. Padding line to push this file past the 300-line ceiling so the audit flags it.
- Component #108: handles a subsystem; see code for details. Padding line to push this file past the 300-line ceiling so the audit flags it.
- Component #109: handles a subsystem; see code for details. Padding line to push this file past the 300-line ceiling so the audit flags it.
- Component #110: handles a subsystem; see code for details. Padding line to push this file past the 300-line ceiling so the audit flags it.
- Component #111: handles a subsystem; see code for details. Padding line to push this file past the 300-line ceiling so the audit flags it.
- Component #112: handles a subsystem; see code for details. Padding line to push this file past the 300-line ceiling so the audit flags it.
- Component #113: handles a subsystem; see code for details. Padding line to push this file past the 300-line ceiling so the audit flags it.
- Component #114: handles a subsystem; see code for details. Padding line to push this file past the 300-line ceiling so the audit flags it.
- Component #115: handles a subsystem; see code for details. Padding line to push this file past the 300-line ceiling so the audit flags it.
- Component #116: handles a subsystem; see code for details. Padding line to push this file past the 300-line ceiling so the audit flags it.
- Component #117: handles a subsystem; see code for details. Padding line to push this file past the 300-line ceiling so the audit flags it.
- Component #118: handles a subsystem; see code for details. Padding line to push this file past the 300-line ceiling so the audit flags it.
- Component #119: handles a subsystem; see code for details. Padding line to push this file past the 300-line ceiling so the audit flags it.
- Component #120: handles a subsystem; see code for details. Padding line to push this file past the 300-line ceiling so the audit flags it.
- Component #121: handles a subsystem; see code for details. Padding line to push this file past the 300-line ceiling so the audit flags it.
- Component #122: handles a subsystem; see code for details. Padding line to push this file past the 300-line ceiling so the audit flags it.
- Component #123: handles a subsystem; see code for details. Padding line to push this file past the 300-line ceiling so the audit flags it.
- Component #124: handles a subsystem; see code for details. Padding line to push this file past the 300-line ceiling so the audit flags it.
- Component #125: handles a subsystem; see code for details. Padding line to push this file past the 300-line ceiling so the audit flags it.
- Component #126: handles a subsystem; see code for details. Padding line to push this file past the 300-line ceiling so the audit flags it.
- Component #127: handles a subsystem; see code for details. Padding line to push this file past the 300-line ceiling so the audit flags it.
- Component #128: handles a subsystem; see code for details. Padding line to push this file past the 300-line ceiling so the audit flags it.
- Component #129: handles a subsystem; see code for details. Padding line to push this file past the 300-line ceiling so the audit flags it.
- Component #130: handles a subsystem; see code for details. Padding line to push this file past the 300-line ceiling so the audit flags it.
- Component #131: handles a subsystem; see code for details. Padding line to push this file past the 300-line ceiling so the audit flags it.
- Component #132: handles a subsystem; see code for details. Padding line to push this file past the 300-line ceiling so the audit flags it.
- Component #133: handles a subsystem; see code for details. Padding line to push this file past the 300-line ceiling so the audit flags it.
- Component #134: handles a subsystem; see code for details. Padding line to push this file past the 300-line ceiling so the audit flags it.
- Component #135: handles a subsystem; see code for details. Padding line to push this file past the 300-line ceiling so the audit flags it.
- Component #136: handles a subsystem; see code for details. Padding line to push this file past the 300-line ceiling so the audit flags it.
- Component #137: handles a subsystem; see code for details. Padding line to push this file past the 300-line ceiling so the audit flags it.
- Component #138: handles a subsystem; see code for details. Padding line to push this file past the 300-line ceiling so the audit flags it.
- Component #139: handles a subsystem; see code for details. Padding line to push this file past the 300-line ceiling so the audit flags it.
- Component #140: handles a subsystem; see code for details. Padding line to push this file past the 300-line ceiling so the audit flags it.
- Component #141: handles a subsystem; see code for details. Padding line to push this file past the 300-line ceiling so the audit flags it.
- Component #142: handles a subsystem; see code for details. Padding line to push this file past the 300-line ceiling so the audit flags it.
- Component #143: handles a subsystem; see code for details. Padding line to push this file past the 300-line ceiling so the audit flags it.
- Component #144: handles a subsystem; see code for details. Padding line to push this file past the 300-line ceiling so the audit flags it.
- Component #145: handles a subsystem; see code for details. Padding line to push this file past the 300-line ceiling so the audit flags it.
- Component #146: handles a subsystem; see code for details. Padding line to push this file past the 300-line ceiling so the audit flags it.
- Component #147: handles a subsystem; see code for details. Padding line to push this file past the 300-line ceiling so the audit flags it.
- Component #148: handles a subsystem; see code for details. Padding line to push this file past the 300-line ceiling so the audit flags it.
- Component #149: handles a subsystem; see code for details. Padding line to push this file past the 300-line ceiling so the audit flags it.
- Component #150: handles a subsystem; see code for details. Padding line to push this file past the 300-line ceiling so the audit flags it.
- Component #151: handles a subsystem; see code for details. Padding line to push this file past the 300-line ceiling so the audit flags it.
- Component #152: handles a subsystem; see code for details. Padding line to push this file past the 300-line ceiling so the audit flags it.
- Component #153: handles a subsystem; see code for details. Padding line to push this file past the 300-line ceiling so the audit flags it.
- Component #154: handles a subsystem; see code for details. Padding line to push this file past the 300-line ceiling so the audit flags it.
- Component #155: handles a subsystem; see code for details. Padding line to push this file past the 300-line ceiling so the audit flags it.
- Component #156: handles a subsystem; see code for details. Padding line to push this file past the 300-line ceiling so the audit flags it.
- Component #157: handles a subsystem; see code for details. Padding line to push this file past the 300-line ceiling so the audit flags it.
- Component #158: handles a subsystem; see code for details. Padding line to push this file past the 300-line ceiling so the audit flags it.
- Component #159: handles a subsystem; see code for details. Padding line to push this file past the 300-line ceiling so the audit flags it.
- Component #160: handles a subsystem; see code for details. Padding line to push this file past the 300-line ceiling so the audit flags it.
- Component #161: handles a subsystem; see code for details. Padding line to push this file past the 300-line ceiling so the audit flags it.
- Component #162: handles a subsystem; see code for details. Padding line to push this file past the 300-line ceiling so the audit flags it.
- Component #163: handles a subsystem; see code for details. Padding line to push this file past the 300-line ceiling so the audit flags it.
- Component #164: handles a subsystem; see code for details. Padding line to push this file past the 300-line ceiling so the audit flags it.
- Component #165: handles a subsystem; see code for details. Padding line to push this file past the 300-line ceiling so the audit flags it.
- Component #166: handles a subsystem; see code for details. Padding line to push this file past the 300-line ceiling so the audit flags it.
- Component #167: handles a subsystem; see code for details. Padding line to push this file past the 300-line ceiling so the audit flags it.
- Component #168: handles a subsystem; see code for details. Padding line to push this file past the 300-line ceiling so the audit flags it.
- Component #169: handles a subsystem; see code for details. Padding line to push this file past the 300-line ceiling so the audit flags it.
- Component #170: handles a subsystem; see code for details. Padding line to push this file past the 300-line ceiling so the audit flags it.
- Component #171: handles a subsystem; see code for details. Padding line to push this file past the 300-line ceiling so the audit flags it.
- Component #172: handles a subsystem; see code for details. Padding line to push this file past the 300-line ceiling so the audit flags it.
- Component #173: handles a subsystem; see code for details. Padding line to push this file past the 300-line ceiling so the audit flags it.
- Component #174: handles a subsystem; see code for details. Padding line to push this file past the 300-line ceiling so the audit flags it.
- Component #175: handles a subsystem; see code for details. Padding line to push this file past the 300-line ceiling so the audit flags it.
- Component #176: handles a subsystem; see code for details. Padding line to push this file past the 300-line ceiling so the audit flags it.
- Component #177: handles a subsystem; see code for details. Padding line to push this file past the 300-line ceiling so the audit flags it.
- Component #178: handles a subsystem; see code for details. Padding line to push this file past the 300-line ceiling so the audit flags it.
- Component #179: handles a subsystem; see code for details. Padding line to push this file past the 300-line ceiling so the audit flags it.
- Component #180: handles a subsystem; see code for details. Padding line to push this file past the 300-line ceiling so the audit flags it.
- Component #181: handles a subsystem; see code for details. Padding line to push this file past the 300-line ceiling so the audit flags it.
- Component #182: handles a subsystem; see code for details. Padding line to push this file past the 300-line ceiling so the audit flags it.
- Component #183: handles a subsystem; see code for details. Padding line to push this file past the 300-line ceiling so the audit flags it.
- Component #184: handles a subsystem; see code for details. Padding line to push this file past the 300-line ceiling so the audit flags it.
- Component #185: handles a subsystem; see code for details. Padding line to push this file past the 300-line ceiling so the audit flags it.
- Component #186: handles a subsystem; see code for details. Padding line to push this file past the 300-line ceiling so the audit flags it.
- Component #187: handles a subsystem; see code for details. Padding line to push this file past the 300-line ceiling so the audit flags it.
- Component #188: handles a subsystem; see code for details. Padding line to push this file past the 300-line ceiling so the audit flags it.
- Component #189: handles a subsystem; see code for details. Padding line to push this file past the 300-line ceiling so the audit flags it.
- Component #190: handles a subsystem; see code for details. Padding line to push this file past the 300-line ceiling so the audit flags it.
- Component #191: handles a subsystem; see code for details. Padding line to push this file past the 300-line ceiling so the audit flags it.
- Component #192: handles a subsystem; see code for details. Padding line to push this file past the 300-line ceiling so the audit flags it.
- Component #193: handles a subsystem; see code for details. Padding line to push this file past the 300-line ceiling so the audit flags it.
- Component #194: handles a subsystem; see code for details. Padding line to push this file past the 300-line ceiling so the audit flags it.
- Component #195: handles a subsystem; see code for details. Padding line to push this file past the 300-line ceiling so the audit flags it.
- Component #196: handles a subsystem; see code for details. Padding line to push this file past the 300-line ceiling so the audit flags it.
- Component #197: handles a subsystem; see code for details. Padding line to push this file past the 300-line ceiling so the audit flags it.
- Component #198: handles a subsystem; see code for details. Padding line to push this file past the 300-line ceiling so the audit flags it.
- Component #199: handles a subsystem; see code for details. Padding line to push this file past the 300-line ceiling so the audit flags it.
- Component #200: handles a subsystem; see code for details. Padding line to push this file past the 300-line ceiling so the audit flags it.
- Component #201: handles a subsystem; see code for details. Padding line to push this file past the 300-line ceiling so the audit flags it.
- Component #202: handles a subsystem; see code for details. Padding line to push this file past the 300-line ceiling so the audit flags it.
- Component #203: handles a subsystem; see code for details. Padding line to push this file past the 300-line ceiling so the audit flags it.
- Component #204: handles a subsystem; see code for details. Padding line to push this file past the 300-line ceiling so the audit flags it.
- Component #205: handles a subsystem; see code for details. Padding line to push this file past the 300-line ceiling so the audit flags it.
- Component #206: handles a subsystem; see code for details. Padding line to push this file past the 300-line ceiling so the audit flags it.
- Component #207: handles a subsystem; see code for details. Padding line to push this file past the 300-line ceiling so the audit flags it.
- Component #208: handles a subsystem; see code for details. Padding line to push this file past the 300-line ceiling so the audit flags it.
- Component #209: handles a subsystem; see code for details. Padding line to push this file past the 300-line ceiling so the audit flags it.
- Component #210: handles a subsystem; see code for details. Padding line to push this file past the 300-line ceiling so the audit flags it.
- Component #211: handles a subsystem; see code for details. Padding line to push this file past the 300-line ceiling so the audit flags it.
- Component #212: handles a subsystem; see code for details. Padding line to push this file past the 300-line ceiling so the audit flags it.
- Component #213: handles a subsystem; see code for details. Padding line to push this file past the 300-line ceiling so the audit flags it.
- Component #214: handles a subsystem; see code for details. Padding line to push this file past the 300-line ceiling so the audit flags it.
- Component #215: handles a subsystem; see code for details. Padding line to push this file past the 300-line ceiling so the audit flags it.
- Component #216: handles a subsystem; see code for details. Padding line to push this file past the 300-line ceiling so the audit flags it.
- Component #217: handles a subsystem; see code for details. Padding line to push this file past the 300-line ceiling so the audit flags it.
- Component #218: handles a subsystem; see code for details. Padding line to push this file past the 300-line ceiling so the audit flags it.
- Component #219: handles a subsystem; see code for details. Padding line to push this file past the 300-line ceiling so the audit flags it.
- Component #220: handles a subsystem; see code for details. Padding line to push this file past the 300-line ceiling so the audit flags it.
- Component #221: handles a subsystem; see code for details. Padding line to push this file past the 300-line ceiling so the audit flags it.
- Component #222: handles a subsystem; see code for details. Padding line to push this file past the 300-line ceiling so the audit flags it.
- Component #223: handles a subsystem; see code for details. Padding line to push this file past the 300-line ceiling so the audit flags it.
- Component #224: handles a subsystem; see code for details. Padding line to push this file past the 300-line ceiling so the audit flags it.
- Component #225: handles a subsystem; see code for details. Padding line to push this file past the 300-line ceiling so the audit flags it.
- Component #226: handles a subsystem; see code for details. Padding line to push this file past the 300-line ceiling so the audit flags it.
- Component #227: handles a subsystem; see code for details. Padding line to push this file past the 300-line ceiling so the audit flags it.
- Component #228: handles a subsystem; see code for details. Padding line to push this file past the 300-line ceiling so the audit flags it.
- Component #229: handles a subsystem; see code for details. Padding line to push this file past the 300-line ceiling so the audit flags it.
- Component #230: handles a subsystem; see code for details. Padding line to push this file past the 300-line ceiling so the audit flags it.
- Component #231: handles a subsystem; see code for details. Padding line to push this file past the 300-line ceiling so the audit flags it.
- Component #232: handles a subsystem; see code for details. Padding line to push this file past the 300-line ceiling so the audit flags it.
- Component #233: handles a subsystem; see code for details. Padding line to push this file past the 300-line ceiling so the audit flags it.
- Component #234: handles a subsystem; see code for details. Padding line to push this file past the 300-line ceiling so the audit flags it.
- Component #235: handles a subsystem; see code for details. Padding line to push this file past the 300-line ceiling so the audit flags it.
- Component #236: handles a subsystem; see code for details. Padding line to push this file past the 300-line ceiling so the audit flags it.
- Component #237: handles a subsystem; see code for details. Padding line to push this file past the 300-line ceiling so the audit flags it.
- Component #238: handles a subsystem; see code for details. Padding line to push this file past the 300-line ceiling so the audit flags it.
- Component #239: handles a subsystem; see code for details. Padding line to push this file past the 300-line ceiling so the audit flags it.
- Component #240: handles a subsystem; see code for details. Padding line to push this file past the 300-line ceiling so the audit flags it.
- Component #241: handles a subsystem; see code for details. Padding line to push this file past the 300-line ceiling so the audit flags it.
- Component #242: handles a subsystem; see code for details. Padding line to push this file past the 300-line ceiling so the audit flags it.
- Component #243: handles a subsystem; see code for details. Padding line to push this file past the 300-line ceiling so the audit flags it.
- Component #244: handles a subsystem; see code for details. Padding line to push this file past the 300-line ceiling so the audit flags it.
- Component #245: handles a subsystem; see code for details. Padding line to push this file past the 300-line ceiling so the audit flags it.
- Component #246: handles a subsystem; see code for details. Padding line to push this file past the 300-line ceiling so the audit flags it.
- Component #247: handles a subsystem; see code for details. Padding line to push this file past the 300-line ceiling so the audit flags it.
- Component #248: handles a subsystem; see code for details. Padding line to push this file past the 300-line ceiling so the audit flags it.
- Component #249: handles a subsystem; see code for details. Padding line to push this file past the 300-line ceiling so the audit flags it.
- Component #250: handles a subsystem; see code for details. Padding line to push this file past the 300-line ceiling so the audit flags it.


Behavioral guidelines to reduce common LLM coding mistakes. Merge with project-specific instructions as needed.

**Tradeoff:** These guidelines bias toward caution over speed. For trivial tasks, use judgment.

## 1. Think Before Coding

**Don't assume. Don't hide confusion. Surface tradeoffs.**

Before implementing:
- State your assumptions explicitly. If uncertain, ask.
- If multiple interpretations exist, present them - don't pick silently.
- If a simpler approach exists, say so. Push back when warranted.
- If something is unclear, stop. Name what's confusing. Ask.

## 2. Simplicity First

**Minimum code that solves the problem. Nothing speculative.**

- No features beyond what was asked.
- No abstractions for single-use code.
- No "flexibility" or "configurability" that wasn't requested.
- No error handling for impossible scenarios.
- If you write 200 lines and it could be 50, rewrite it.

Ask yourself: "Would a senior engineer say this is overcomplicated?" If yes, simplify.

## 3. Surgical Changes

**Touch only what you must. Clean up only your own mess.**

When editing existing code:
- Don't "improve" adjacent code, comments, or formatting.
- Don't refactor things that aren't broken.
- Match existing style, even if you'd do it differently.
- If you notice unrelated dead code, mention it - don't delete it.

When your changes create orphans:
- Remove imports/variables/functions that YOUR changes made unused.
- Don't remove pre-existing dead code unless asked.

The test: Every changed line should trace directly to the user's request.

## 4. Goal-Driven Execution

**Define success criteria. Loop until verified.**

Transform tasks into verifiable goals:
- "Add validation" → "Write tests for invalid inputs, then make them pass"
- "Fix the bug" → "Write a test that reproduces it, then make it pass"
- "Refactor X" → "Ensure tests pass before and after"

For multi-step tasks, state a brief plan:
```
1. [Step] → verify: [check]
2. [Step] → verify: [check]
3. [Step] → verify: [check]
```

Strong success criteria let you loop independently. Weak criteria ("make it work") require constant clarification.

---

**These guidelines are working if:** fewer unnecessary changes in diffs, fewer rewrites due to overcomplication, and clarifying questions come before implementation rather than after mistakes.
